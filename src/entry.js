// Entry Point
  return {
    init:     init,
    reset:    reset,
    config:   config,
    utils:    pivotUtils,
    csv:      processCSV,
    json:     processJSON,
    data:     pivotData,
    results:  getDataResults,
    fields:   pivotFields,
    filters:  pivotFilters,
    display:  pivotDisplay
  }